{% extends "layout.njk" %}

{% block head %}
    <title>Foodfy</title>
{% endblock %}

{% block wrapper %}
    <section class="contain-index">
        <article class="text-contain">
            <div class="description">
                <h1>As melhores receitas</h1>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
            </div>
        </article>
        <div class="img-contain">
            <img src="assets/img/chef.png" alt="Chef">
        </div>
    </section>
    <section class="accessed">
        <h1>Mais acessadas</h1>
        <div class="most-accessed">
            {% for card in cards %}
                <div class="card-accessed">
                    <div class="img-accessed">
                        <img src="{{card.smallImage}}" alt="{{card.idRecipe}}">
                    </div>
                    <div class="card-content-accessed">
                        <h3>{{card.title}}</h3>
                        <span class="author">por {{card.author}}</span>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}